# Protocol Files

This repository vendors the SSL simulation protocol files needed by
`ssl-sim-proto`.

The copied files live in:

```text
protocol/ssl-simulation-protocol/proto
```

You can still set `SSL_SIMULATION_PROTO_DIR` to point at another local checkout
when testing against a newer draft.
